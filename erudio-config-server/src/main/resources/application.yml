#Erudio-config-server
server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pedrohjc/erudio-config-server
          default-label: main
          search-paths: 'greeting-service*'